json.extract! mnto, :id, :code, :division_id, :ocupacion_id, :fecha, :truck_id, :km_programado, :km_actual, :fecha2, :user_id, :processed, :date_processed, :created_at, :updated_at
json.url mnto_url(mnto, format: :json)
